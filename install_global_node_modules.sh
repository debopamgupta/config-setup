#! /bin/bash

echo 'node and npm must be installed....'

npm i live-server -g
npm i lite-server -g
npm i -g reveal-md

echo 'installing vercel (zeit) now CLI'
npm i -g now
echo 'Do now login after setup is over'
npm i -g tldr && tldr --update

curl -o- -L https://yarnpkg.com/install.sh | bash

cd ~ && mkdir Dev  

echo "logging in to now "
now login
echo "logging in to heroku "
heroku login
